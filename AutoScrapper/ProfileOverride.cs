namespace AutoScrapper
{
    /// <summary>
    /// An enum that represents the different profile overrides.
    /// This can be used to quickly swap between different profiles
    /// </summary>
    public enum ProfileOverride
    {
        None, // Use the main config
        Profile1,
        Profile2,
        Profile3,
    }
}